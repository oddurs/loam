# Fences

````markdown
```
# not a heading: the inner fence is shorter
```
# still not a heading
````

```js `not a fence`
# a heading: the backtick line above was not a fence

~~~
# not a heading
```
# not closed by backticks
~~~~

## Visible

```
# never closed, to the end
