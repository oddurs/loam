# Pinned so a contributor, an agent and CI all compile with the same compiler,
# and to the same version harrow uses. Bumping it is a commit of its own, with
# CI green on the new version.
[toolchain]
channel = "1.98"
components = ["rustfmt", "clippy"]
